rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Development rules - allow all access for testing
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}